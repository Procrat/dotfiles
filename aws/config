[profile personal]
credential_process = aws-vault --backend=pass --pass-prefix=aws-cli export --format=json personal

[profile restic]
credential_process = aws-vault --backend=pass --pass-prefix=aws-cli export --format=json restic
