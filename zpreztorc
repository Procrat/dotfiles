# Order is important!
zstyle ':prezto:load' pmodule \
  'environment' \
  'terminal' \
  'editor' \
  'history' \
  'directory' \
  'command-not-found' \
  'completion' \
  'syntax-highlighting' \
  'prompt'

# TODO Checkout the bloated ones:
# utility, completion, python, rails, prompt
# Remember to check out the order in /usr/lib/prezto/runcoms/...

# Set extra vim bindings and prompt interaction
zstyle ':prezto:module:editor' key-bindings 'vi'
bindkey -M viins jj vi-cmd-mode

zstyle ':prezto:*:*' color 'yes'

# Don't do cursor highlighting
zstyle ':prezto:module:syntax-highlighting' highlighters 'main'

# vim:filetype=zsh
