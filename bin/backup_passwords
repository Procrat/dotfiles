#!/usr/bin/env bash
#
# Backs up the PGP encrypted password files to Dropbox

set -euo pipefail

tar -cz .password-store \
    | gpg --sign --encrypt -r 'Stijn Seghers' \
    > ~/dropbox/.password-store-backup.tar.gz.gpg
