#!/bin/bash

set -euo pipefail


height="$(($(cat $HOME/.config/herbstluftwm/dzen/panel_height) - 1))"
screen_geometry="$(wmctrl -d | awk '/^...\*/ { print $9 }')"
width="$((${screen_geometry%x*} - 450))"

exec yeganesh -- -i -h "$height" -x 55 -w "$width" "$@"
