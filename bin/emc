#!/usr/bin/env bash
#
# Tests wether we live in a terminal or in a graphical environment and opens
# Emacs accordingly.

set -euo pipefail

if tty &>/dev/null; then
    options="-t"
else
    options="-nc"
fi

exec emacsclient -a "" "$options" "$@"
