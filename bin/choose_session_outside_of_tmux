#!/bin/bash
#
# Let user choose a tmux to attach without having to have a client running.

set -euo pipefail

chosen_session="$(tmux ls -F '#{session_name}' | "$HOME/bin/mydmenu")"
if [[ -n "$chosen_session" ]]; then
    exec tmux attach -t "$chosen_session"
fi
